<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="api">
	<insert id="searchWeatherLogInsert">
		insert into tb_search_record_info(
			user_id,
		    record_date,
		    search_start_date,
		    search_end_date)
		values(
			#{user_id},
			now(), 
			#{startdate}, 
			#{enddate}
			);
	</insert>
</mapper>
